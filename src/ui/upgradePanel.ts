// Per-run upgrade panel (VISUAL_SPEC §7). Stub for milestone 3.

import type { Store } from "../state/store.ts";
import type { GameState } from "../state/schema.ts";

export function mountUpgradePanel(
  _parent: HTMLElement,
  _store: Store<GameState>,
): void {
  // Milestone 3: render upgrades from upgrades.ts with buy intents.
}
